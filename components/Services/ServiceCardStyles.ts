import tw from 'tailwind-styled-components';
import { IconType } from 'react-icons';

export const ServiceCardContainer = tw.div`
    flex 
    items-center 
    p-2 space-x-4 
`;

// export const IconContainer = tw(IconType)``
